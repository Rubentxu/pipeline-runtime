pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'pipeline-runtime'
include 'core', 'examples', 'pipeline-cli'


enableFeaturePreview('GROOVY_COMPILATION_AVOIDANCE')

